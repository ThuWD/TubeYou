document.body.addEventListener("keydown", function (event) {
    if (event.keyCode === 27) {
        window.location.replace("/TubeYou/App");
        alert('welcome');
        alert('the games have only just begun');
    }
});
